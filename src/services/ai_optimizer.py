"""
AI Optimizer: Self-learning system for parameter tuning and strategy adaptation.
Uses genetic algorithm and reinforcement learning concepts.
\"\"\"\n\nimport json\nimport logging\nimport random\nfrom dataclasses import dataclass, asdict\nfrom typing import Dict, List, Optional, Tuple\nimport numpy as np\nfrom datetime import datetime\n\nlogger = logging.getLogger(__name__)\n\n\n@dataclass\nclass StrategyGenome:\n    \"\"\"Represents a strategy configuration as a genome.\"\"\"\n    strategy_name: str\n    parameters: Dict\n    fitness_score: float = 0.0\n    trades_count: int = 0\n    winrate: float = 0.0\n    profit_factor: float = 0.0\n    sharpe_ratio: float = 0.0\n    max_drawdown: float = 0.0\n    creation_time: str = None\n    \n    def __post_init__(self):\n        if self.creation_time is None:\n            self.creation_time = datetime.now().isoformat()\n    \n    def calculate_fitness(self) -> float:\n        \"\"\"\n        Calculate fitness score based on performance metrics.\n        Fitness = (Winrate * 0.3) + (Profit Factor * 0.3) + (Sharpe Ratio * 0.2) + (Max Drawdown * -0.2)\n        \"\"\"\n        fitness = (\n            (self.winrate / 100 * 0.3) +\n            (min(self.profit_factor, 5.0) / 5.0 * 0.3) +\n            (min(self.sharpe_ratio, 3.0) / 3.0 * 0.2) +\n            (max(0, 1 - abs(self.max_drawdown) / 100) * 0.2)\n        )\n        self.fitness_score = fitness\n        return fitness\n\n\nclass GeneticAlgorithm:\n    \"\"\"Genetic algorithm for strategy optimization.\"\"\"\n    \n    def __init__(\n        self,\n        population_size: int = 20,\n        mutation_rate: float = 0.1,\n        crossover_rate: float = 0.8,\n        elite_size: int = 2,\n    ):\n        \"\"\"\n        Initialize genetic algorithm.\n        \n        Args:\n            population_size: Size of population\n            mutation_rate: Probability of mutation\n            crossover_rate: Probability of crossover\n            elite_size: Number of elite individuals to preserve\n        \"\"\"\n        self.population_size = population_size\n        self.mutation_rate = mutation_rate\n        self.crossover_rate = crossover_rate\n        self.elite_size = elite_size\n        self.population: List[StrategyGenome] = []\n        self.generation = 0\n    \n    def initialize_population(\n        self,\n        strategy_name: str,\n        parameter_ranges: Dict,\n    ):\n        \"\"\"\n        Initialize population with random parameters.\n        \n        Args:\n            strategy_name: Name of strategy\n            parameter_ranges: Dict with parameter ranges {param: (min, max)}\n        \"\"\"\n        self.population = []\n        \n        for _ in range(self.population_size):\n            params = {}\n            for param, (min_val, max_val) in parameter_ranges.items():\n                if isinstance(min_val, int):\n                    params[param] = random.randint(min_val, max_val)\n                else:\n                    params[param] = random.uniform(min_val, max_val)\n            \n            genome = StrategyGenome(\n                strategy_name=strategy_name,\n                parameters=params,\n            )\n            self.population.append(genome)\n        \n        logger.info(f\"Initialized population with {self.population_size} genomes\")\n    \n    def evaluate_fitness(self, performance_metrics: List[Dict]):\n        \"\"\"\n        Evaluate fitness for all genomes based on performance metrics.\n        \n        Args:\n            performance_metrics: List of performance metrics for each genome\n        \"\"\"\n        for i, metrics in enumerate(performance_metrics):\n            if i < len(self.population):\n                genome = self.population[i]\n                genome.trades_count = metrics.get('trades_count', 0)\n                genome.winrate = metrics.get('winrate', 0)\n                genome.profit_factor = metrics.get('profit_factor', 0)\n                genome.sharpe_ratio = metrics.get('sharpe_ratio', 0)\n                genome.max_drawdown = metrics.get('max_drawdown', 0)\n                genome.calculate_fitness()\n    \n    def select_parents(self) -> Tuple[StrategyGenome, StrategyGenome]:\n        \"\"\"\n        Select two parents using tournament selection.\n        \n        Returns:\n            Tuple of two parent genomes\n        \"\"\"\n        tournament_size = 3\n        \n        def tournament():\n            tournament_pop = random.sample(self.population, tournament_size)\n            return max(tournament_pop, key=lambda x: x.fitness_score)\n        \n        parent1 = tournament()\n        parent2 = tournament()\n        \n        return parent1, parent2\n    \n    def crossover(self, parent1: StrategyGenome, parent2: StrategyGenome) -> StrategyGenome:\n        \"\"\"\n        Create offspring through crossover.\n        \n        Args:\n            parent1: First parent genome\n            parent2: Second parent genome\n        \n        Returns:\n            Offspring genome\n        \"\"\"\n        offspring_params = {}\n        \n        for param in parent1.parameters:\n            if random.random() < 0.5:\n                offspring_params[param] = parent1.parameters[param]\n            else:\n                offspring_params[param] = parent2.parameters[param]\n        \n        offspring = StrategyGenome(\n            strategy_name=parent1.strategy_name,\n            parameters=offspring_params,\n        )\n        \n        return offspring\n    \n    def mutate(self, genome: StrategyGenome, parameter_ranges: Dict):\n        \"\"\"\n        Mutate genome parameters.\n        \n        Args:\n            genome: Genome to mutate\n            parameter_ranges: Parameter ranges for mutation\n        \"\"\"\n        for param in genome.parameters:\n            if random.random() < self.mutation_rate:\n                if param in parameter_ranges:\n                    min_val, max_val = parameter_ranges[param]\n                    if isinstance(min_val, int):\n                        genome.parameters[param] = random.randint(min_val, max_val)\n                    else:\n                        genome.parameters[param] = random.uniform(min_val, max_val)\n    \n    def evolve(self, parameter_ranges: Dict) -> List[StrategyGenome]:\n        \"\"\"\n        Evolve population for one generation.\n        \n        Args:\n            parameter_ranges: Parameter ranges for mutation\n        \n        Returns:\n            New population\n        \"\"\"\n        # Sort by fitness\n        self.population.sort(key=lambda x: x.fitness_score, reverse=True)\n        \n        # Preserve elite\n        new_population = self.population[:self.elite_size].copy()\n        \n        # Generate offspring\n        while len(new_population) < self.population_size:\n            parent1, parent2 = self.select_parents()\n            \n            if random.random() < self.crossover_rate:\n                offspring = self.crossover(parent1, parent2)\n            else:\n                offspring = random.choice([parent1, parent2])\n            \n            self.mutate(offspring, parameter_ranges)\n            new_population.append(offspring)\n        \n        self.population = new_population[:self.population_size]\n        self.generation += 1\n        \n        logger.info(\n            f\"Generation {self.generation}: Best fitness = {self.population[0].fitness_score:.4f}\"\n        )\n        \n        return self.population\n    \n    def get_best_genome(self) -> StrategyGenome:\n        \"\"\"Get best genome in population.\"\"\"\n        return max(self.population, key=lambda x: x.fitness_score)\n\n\nclass AIOptimizer:\n    \"\"\"Main AI optimizer for strategy parameter tuning.\"\"\"\n    \n    def __init__(\n        self,\n        population_size: int = 20,\n        mutation_rate: float = 0.1,\n    ):\n        \"\"\"Initialize AI optimizer.\"\"\"\n        self.ga = GeneticAlgorithm(\n            population_size=population_size,\n            mutation_rate=mutation_rate,\n        )\n        self.optimization_history: List[Dict] = []\n        self.best_genomes: Dict[str, StrategyGenome] = {}\n    \n    def get_parameter_ranges(self, strategy_name: str) -> Dict:\n        \"\"\"\n        Get parameter ranges for a strategy.\n        \n        Args:\n            strategy_name: Name of strategy\n        \n        Returns:\n            Dict with parameter ranges\n        \"\"\"\n        ranges = {\n            'EMAcrossover': {\n                'fast_period': (5, 15),\n                'slow_period': (20, 50),\n                'min_volume': (50000, 200000),\n            },\n            'RSIOversold': {\n                'period': (10, 20),\n                'oversold_threshold': (20, 35),\n                'overbought_threshold': (65, 80),\n            },\n            'BollingerBands': {\n                'period': (15, 30),\n                'std_dev': (1.5, 3.0),\n            },\n            'MACD': {\n                'fast': (10, 15),\n                'slow': (20, 30),\n                'signal': (7, 12),\n            },\n            'AlphaSignals': {\n                'ema_fast': (5, 15),\n                'ema_slow': (20, 50),\n                'rsi_period': (10, 20),\n                'bb_period': (15, 30),\n                'bb_std': (1.5, 3.0),\n            },\n            'PriceAction': {\n                'lookback': (10, 30),\n                'breakout_threshold': (1.01, 1.05),\n            },\n        }\n        \n        return ranges.get(strategy_name, {})\n    \n    def start_optimization(self, strategy_name: str):\n        \"\"\"\n        Start optimization for a strategy.\n        \n        Args:\n            strategy_name: Name of strategy to optimize\n        \"\"\"\n        parameter_ranges = self.get_parameter_ranges(strategy_name)\n        self.ga.initialize_population(strategy_name, parameter_ranges)\n        logger.info(f\"Started optimization for {strategy_name}\")\n    \n    def update_performance(\n        self,\n        performance_metrics: List[Dict],\n    ):\n        \"\"\"\n        Update performance metrics for current population.\n        \n        Args:\n            performance_metrics: List of performance metrics\n        \"\"\"\n        self.ga.evaluate_fitness(performance_metrics)\n    \n    def evolve(self, strategy_name: str) -> List[Dict]:\n        \"\"\"\n        Evolve population for one generation.\n        \n        Args:\n            strategy_name: Name of strategy\n        \n        Returns:\n            List of new parameter configurations\n        \"\"\"\n        parameter_ranges = self.get_parameter_ranges(strategy_name)\n        new_population = self.ga.evolve(parameter_ranges)\n        \n        # Record best genome\n        best = self.ga.get_best_genome()\n        self.best_genomes[strategy_name] = best\n        \n        # Record history\n        self.optimization_history.append({\n            'timestamp': datetime.now().isoformat(),\n            'strategy': strategy_name,\n            'generation': self.ga.generation,\n            'best_fitness': best.fitness_score,\n            'best_parameters': best.parameters,\n        })\n        \n        # Return new configurations\n        return [\n            {\n                'parameters': genome.parameters,\n                'expected_fitness': genome.fitness_score,\n            }\n            for genome in new_population\n        ]\n    \n    def get_best_parameters(self, strategy_name: str) -> Optional[Dict]:\n        \"\"\"\n        Get best parameters for a strategy.\n        \n        Args:\n            strategy_name: Name of strategy\n        \n        Returns:\n            Best parameters or None\n        \"\"\"\n        best_genome = self.best_genomes.get(strategy_name)\n        if best_genome:\n            return best_genome.parameters\n        return None\n    \n    def get_optimization_stats(self) -> Dict:\n        \"\"\"\n        Get optimization statistics.\n        \n        Returns:\n            Dict with optimization stats\n        \"\"\"\n        return {\n            'total_generations': self.ga.generation,\n            'best_genomes': {\n                name: {\n                    'fitness': genome.fitness_score,\n                    'parameters': genome.parameters,\n                    'metrics': {\n                        'winrate': genome.winrate,\n                        'profit_factor': genome.profit_factor,\n                        'sharpe_ratio': genome.sharpe_ratio,\n                        'max_drawdown': genome.max_drawdown,\n                    },\n                }\n                for name, genome in self.best_genomes.items()\n            },\n            'history_length': len(self.optimization_history),\n        }\n\n\nasync def main():\n    \"\"\"Main entry point for AI optimizer.\"\"\"\n    logging.basicConfig(\n        level=logging.INFO,\n        format='%(asctime)s - %(name)s - %(levelname)s - %(message)s',\n    )\n    \n    # Example usage\n    optimizer = AIOptimizer(population_size=10, mutation_rate=0.15)\n    \n    # Start optimization for EMA strategy\n    optimizer.start_optimization('EMAcrossover')\n    \n    # Simulate performance metrics\n    for generation in range(5):\n        # Mock performance data\n        performance = [\n            {\n                'trades_count': random.randint(10, 50),\n                'winrate': random.uniform(40, 70),\n                'profit_factor': random.uniform(1.0, 2.5),\n                'sharpe_ratio': random.uniform(0.5, 2.0),\n                'max_drawdown': random.uniform(-10, 0),\n            }\n            for _ in range(10)\n        ]\n        \n        optimizer.update_performance(performance)\n        new_configs = optimizer.evolve('EMAcrossover')\n        \n        logger.info(f\"Generation {generation + 1}: Generated {len(new_configs)} new configurations\")\n    \n    # Print results\n    stats = optimizer.get_optimization_stats()\n    logger.info(f\"Optimization stats: {json.dumps(stats, indent=2)}\")\n\n\nif __name__ == '__main__':\n    import asyncio\n    asyncio.run(main())\n
